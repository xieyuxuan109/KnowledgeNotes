### 初始化数据库连接池
```
package dao

import (
	"fmt"
	"time"

	"gorm.io/driver/mysql"
	"gorm.io/gorm"
)

type Config struct {
	Username        string
	Password        string
	Host            string
	Port            string
	Database        string
	MaxIdleConns    int           // 最大空闲连接数，默认 10
	MaxOpenConns    int           // 最大打开连接数，默认 100
	ConnMaxLifetime time.Duration // 连接最大存活时间，默认 1h
	ConnMaxIdleTime time.Duration // 连接最大空闲时间，默认 10m
}

func DSN(config Config) (*gorm.DB, error) {
	if config.MaxIdleConns <= 0 {
		config.MaxIdleConns = 10
	}
	if config.MaxOpenConns <= 0 {
		config.MaxOpenConns = 100
	}
	if config.ConnMaxLifetime <= 0 {
		config.ConnMaxLifetime = time.Hour
	}
	if config.ConnMaxIdleTime <= 0 {
		config.ConnMaxIdleTime = 10 * time.Minute
	}
	dsn := fmt.Sprintf("%s:%s@(%s:%s)/%s?charset=utf8mb4&parseTime=True&loc=Local",
		config.Username, config.Password, config.Host, config.Port, config.Database)
	DB, err := gorm.Open(mysql.Open(dsn), &gorm.Config{})
	if err != nil {
		return nil, fmt.Errorf("连接数据库失败: %w, DSN: %s", err, dsn)
	}
	sqlDB, err := DB.DB()
	if err != nil {
		return nil, fmt.Errorf("获取数据库实例失败: %w", err)
	}

	// 配置连接池
	sqlDB.SetMaxIdleConns(config.MaxIdleConns)
	sqlDB.SetMaxOpenConns(config.MaxOpenConns)
	sqlDB.SetConnMaxLifetime(config.ConnMaxLifetime)
	sqlDB.SetConnMaxIdleTime(config.ConnMaxIdleTime)

	fmt.Printf("数据库连接池已配置: MaxIdle=%d, MaxOpen=%d, MaxLifetime=%s, MaxIdleTime=%s\n",
		config.MaxIdleConns, config.MaxOpenConns, config.ConnMaxLifetime, config.ConnMaxIdleTime)

	return DB, nil
}
```
### 初始化协程池
```
package dao

import (
	"fmt"
	"time"

	"gorm.io/driver/mysql"
	"gorm.io/gorm"
)

type Config struct {
	Username        string
	Password        string
	Host            string
	Port            string
	Database        string
	MaxIdleConns    int           // 最大空闲连接数，默认 10
	MaxOpenConns    int           // 最大打开连接数，默认 100
	ConnMaxLifetime time.Duration // 连接最大存活时间，默认 1h
	ConnMaxIdleTime time.Duration // 连接最大空闲时间，默认 10m
}

func DSN(config Config) (*gorm.DB, error) {
	if config.MaxIdleConns <= 0 {
		config.MaxIdleConns = 10
	}
	if config.MaxOpenConns <= 0 {
		config.MaxOpenConns = 100
	}
	if config.ConnMaxLifetime <= 0 {
		config.ConnMaxLifetime = time.Hour
	}
	if config.ConnMaxIdleTime <= 0 {
		config.ConnMaxIdleTime = 10 * time.Minute
	}
	dsn := fmt.Sprintf("%s:%s@(%s:%s)/%s?charset=utf8mb4&parseTime=True&loc=Local",
		config.Username, config.Password, config.Host, config.Port, config.Database)
	DB, err := gorm.Open(mysql.Open(dsn), &gorm.Config{})
	if err != nil {
		return nil, fmt.Errorf("连接数据库失败: %w, DSN: %s", err, dsn)
	}
	sqlDB, err := DB.DB()
	if err != nil {
		return nil, fmt.Errorf("获取数据库实例失败: %w", err)
	}

	// 配置连接池
	sqlDB.SetMaxIdleConns(config.MaxIdleConns)
	sqlDB.SetMaxOpenConns(config.MaxOpenConns)
	sqlDB.SetConnMaxLifetime(config.ConnMaxLifetime)
	sqlDB.SetConnMaxIdleTime(config.ConnMaxIdleTime)

	fmt.Printf("数据库连接池已配置: MaxIdle=%d, MaxOpen=%d, MaxLifetime=%s, MaxIdleTime=%s\n",
		config.MaxIdleConns, config.MaxOpenConns, config.ConnMaxLifetime, config.ConnMaxIdleTime)

	return DB, nil
}
```