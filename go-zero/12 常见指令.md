## 用于快速创建一个新的 API 服务，服务名为 shorturl
```
goctl api new shorturl 
```
## 重置本地自定义模板清理
```
PS C:\workspace\ShortURL> goctl template clean
templates are cleaned!
```
## 如果更新了api文件想要重新生成
```
先删除internal文件夹
然后执行
goctl api go -api shorturl.api -dir .
```