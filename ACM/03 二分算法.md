## 二分算法
```
细节：
    while循环里面判断如何写 while(left<right) or while(left<=right)
    求中点方式 while(left+right)/2 or while((left+right+1)/2)
    二分结束后需要判断一下相遇点的情况
```
## STL库函数
```
lower_bound 大于等于x的最小元素 返回的是迭代器 左闭右开
upper_bound 大于x的最小元素 返回的是迭代器
```