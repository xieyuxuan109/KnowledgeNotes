syntax = "proto3";

package proto;
option go_package = "add_client/proto";
//定义rpc服务
service CalcService{
    rpc Add(AddRequest)returns(AddResponse){}
}
//定义消息
message AddRequest {
  int32 x = 1;
  int32 y=2;
}
message AddResponse {
  int32 result = 1;
}
