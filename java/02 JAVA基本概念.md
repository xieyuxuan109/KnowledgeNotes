## 标识符命名规则
```
硬性要求
由数字 字母 下划线_ 和美元符$组成
不能由数字开头
不能是关键字
区分大小写
```
```
软性建议
见名知意 驼峰命名
```
## 键盘读入
```
public class HelloWorld {
    static void main() {
        // 1.找到scanner 类
        Scanner sc = new Scanner(System.in);
        //2.让Scanner干活
        //接收键盘录入整数
        int num=sc.nextInt();//固定格式
        System.out.println(num);
        //接收键盘录入小数
        double num2 =sc.nextDouble();
        System.out.println(num2);
        //接收键盘录入文本 字符串
        String num3=sc.next();
        System.out.println(num3);
    }
}
```
## 运算
```
注意小数参与运算可能不精确
整数相除结果是整数
```
## 类型转换
```
JAVA会默认采取隐式转换
但注意 如果由byte short类型的数据 会提升为int类型再进行运算
byte a = 10;
byte b = 20;
int c = a + b;
```
```
也可以进行强制类型转换
int a = 10;
byte b=(byte)a;
```
## 逻辑运算符
```
& | ! 与 或 非 
```
## 方法
```
方法是指程序中的独立功能 也是最小执行单元
```
```
public class HelloWorld{
    public static void main(String[] args){
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        int b = sc.nextInt();
        System.out.println(add(a,b));
    }
    public static int add(int a,int b){
        return a+b;
    }
}
```
## 方法的重载
```
方法名一样但是传的形参不一样 参数看 个数 类型 顺序不同
```
## java运行原理篇
```
java文件 =》 class文件 =》 JVM虚拟机 =》 操作系统
内存 软件在运行时用来临时存储数据的
栈 堆（new） 方法区字节码文件
```
## 面向对象 结构体
```
类是模板 对象是根据模板创建出来的实例
对象 把相关数据和方法组织为一个整体来看待
```
```
java bean类 是用来描述一类事物的类 行为和属性
有main方法的类叫做测试类
```
```
private 关键字修饰过后只有本类可以访问 可以通过get和set方法进行修改
```
```
使用变量符合就近原则
成员变量和局部变量
成员变量 类里面 方法外面
局部变量 类里面 方法里面
this可以在方法里面突破局部变量使用成员变量
this本质代表所在方法调用者的内存地址
```
```
构造方法 会自动匹配
方法名与类名相同 大小写也要一致
没有返回值类型 void都没有
没有具体的返回值 即不能return返回结果数据

执行时机
每次创建对象的时候由虚拟机自动调用 不能手动调用构造方法
每创建一次对象 就会调用一次构造方法
```
```
构造方法注意事项
如果没有定义构造方法，系统将给出一个默认的无参数构造方法
如果自己写了任意构造方法，系统将不再提供默认的构造方法
带参构造方法和无参数构造方法，两者方法名相同，但是参数不同，这叫做构造方法的重载
习惯：无论是否使用，都手动书写无参数构造方法，和带全部参数的构造方法
```
```
static 关键字 修饰成员变量
static 叫做静态变量，被该类所有对象共享
static 修饰成员方法
static修饰的方法叫做静态方法 多用于测试类和工具类中
Javabean类中很少会用
调用方式 方式一 类名调用
        方式二 对象名调用
```
```
工具类
类名见名知意 私有化构造方法
```
```
静态的注意事项
静态方法只能访问静态变量和其他的静态方法
非静态方法可以访问静态变量或者静态方法，也可以访问非静态的成员变量和非静态的成员方法
静态方法中是没有this关键字
总结：静态只能调用静态，非静态可以调用所有，静态方法无this
```
```
定义常量 final 全部大写 多个单词用下划线隔开 只可以赋值一次
final修饰基本数据类型 其值不能改变 修饰引用数据类型 其内存地址不能改变 
```
```
枚举 对象个数是有限个
public enum 枚举类名{}
```
```
继承 extends
java只支持单继承 不支持多继承 但支持多层继承
最开始的类Object类 找寻比变量依旧遵循就近原则
this本类 super父类
```
```
方法的重写 当父类的方法满足不了子类需求需要重写一遍方法 Alt+Insert
@Override 和 方法名保持一致
```
```
父类的构造方法不会被子类继承 只能通过super关键字被调用 super()
子类构造方法第一行默认会有super（），访问父类无参构造
访问有参构造必须手动写super(1,2)
```
```
添加默认值
1.提前再javabeen里面赋值
2.this（）调用本类其他构造方法 子类有多个构造方法时不能用this互相调用
  一定要预留一个调用父类的构造方法
  一旦构造方法写上了this(),就不能再写super（），JVM也不会自动添加super()
```
```
子类真正能继承父类的是什么？
构造方法：不能被子类继承，可以利用super关键字调用
成员变量：可以被子类继承，private私有的也可以，但是私有的无法直接调用
成员方法：虚方法可以被继承
final修饰的最终方法不能被继承，可以被调用
static修饰的静态方法不能被继承，可以被调用
private修饰的私有方法不能被继承，不能被调用
```
```
java中的权限修饰符
```
| 修饰符 | 同一个类 | 本包中其他类 | 不同包下的子类 | 不同包下的无关类 |
| :--- | :--- | :--- | :--- | :--- |
| private | √ | | | |
| 空着/缺省/默认 | √ | √ | | |
| protected | √ | √ | √ | |
| public | √ | √ | √ | √ |
```
多态
事物的多种形态
父类类型 对象名称 = 子类对象
多态的前提 由继承/实现关系 必须
需要有父类引用指向子类对象 必须
子类中有父类方法的重写 可选 //重写了的话调用子类的方法
```
```
多态特点
变量调用 编译看左边 运行也看左边
方法调用 编译看左边 运行看右边（如果子类没有重写方法，那么就用父类的方法）
多态的弊端 不能调用子类独有的方法
```
```
解决多态的弊端
类型转换
自动类型转换（从子到父）： 子类对象赋值给父类类型的变量
强制类型转换（从父到子）： 子类 对象变量 = (子类)父类类型的变量
Person p = new Student();
Student s = (Student)p;
只能转成自己的类型 例如爸爸赋值给了爷爷 那只能强转为爸爸 不能强转为儿子
y instance Fu 判断y是不是父类类型
```
```
注意点：
抽象类不能实例化
如果可以创建抽象类的对象的话，用对象调用一个没有方法体的抽象方法，没有任何意义。
抽象类中不一定有抽象方法
作用：为了不让外界创建本类的对象
有抽象方法的类一定是抽象类（正常的内容）
抽象类中可以有构造方法
作用：为了给成员变量赋值
抽象类的子类
要么重写抽象类中的所有抽象方法（正常内容）
要么子类也是抽象类
这种方式意义不大，还要写一个孙子类继承子类，并且重写所有的抽象方法。
在外界才可以创建孙子类的对象，调用里面的方法
```
```
接口
接口: 接口就是一个规则，而且是独立于继承体系以外的规则。（可以理解为干爹）
格式: 关键字interface来定义
public interface 接口名{...}
使用: 接口和类之间的是实现关系，通过implements关键字表示
public class 类名 implements 接口{...}
注意点1: 接口不能实例化
注意点2: 接口的子类（实现类），要么重写接口中所有的抽象方法，要么实现类是一个抽象类
注意点3: 一个类可以实现多个接口，也可以在继承一个类的同时，实现多个接口
public class 类名 implements 接口1, 接口2{...}
public class 类名 extends 父类名 implements 接口1, 接口2{...}
反编译 javap class文件名
```
```
接口中成员的特点
成员变量：只能是常量。默认修饰符：public static final
构造方法：没有
成员方法：只能是抽象方法。默认修饰符：public abstract
JDK7以前：接口中只能定义抽象方法
JDK8的新特性：接口中可以定义有方法体的方法
JDK9的新特性：接口中可以定义私有方法
```
```
接口和类之间的关系
类和类的关系
继承关系，只能单继承，不能多继承，但是可以多层继承
类和接口的关系
实现关系，可以单实现，也可以多实现，还可以在继承一个类的同时实现多个接口
接口和接口的关系
继承关系，可以单继承，也可以多继承
```
```
内部类 就是定义再一个类里面的类
内部类表示的事物是外部类的一部分
内部类单独出现没有任何意义
```
```
静态内部类
静态内部类只能访问外部类中的静态变量和静态方法
如果在静态内部类中，想要访问外部类非静态的内容，需要创建外部类的对象
创建静态内部类的对象：
外部类名.内部类名 对象名 = new 外部类名.内部类名();
调用静态内部类中非静态方法的格式：
先创建对象，用对象调用
调用静态内部类中静态方法的格式：
外部类名.内部类名.方法名();
```
```
局部内部类
将内部类定义在方法里面就叫做局部内部类，类似于方法里面的局部变量。
外界是无法直接使用局部内部类，需要在方法内部创建对象并使用。
该类可以直接访问外部类的成员，也可以访问方法内的局部变量。 
```
```
匿名内部类
```