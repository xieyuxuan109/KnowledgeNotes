### 相关依赖安装
```
yarn add vue-router -D开发环境/-S生产环境
```
```
yarn add v1.22.22
info No lockfile found.
warning package-lock.json found. Your project contains lock files generated by tools other than Yarn. It is advised not to mix package managers in order to avoid resolution inconsistencies caused by unsynchronized lock files. To clear this warning, remove package-lock.json.
[1/4] Resolving packages...
warning cnpm > npminstall > node-gyp > glob@7.2.3: Glob versions prior to v9 are no longer supported
warning cnpm > npm > npmlog@7.0.1: This package is no longer supported.
warning cnpm > npminstall > @npmcli/arborist > npmlog@7.0.1: This package is no longer supported.
warning cnpm > npminstall > node-gyp > npmlog@6.0.2: This package is no longer supported.
warning cnpm > npm > libnpmexec > npmlog@7.0.1: This package is no longer supported.
warning cnpm > npminstall > node-gyp > make-fetch-happen > cacache > glob@8.1.0: Glob versions prior to v9 are no longer supported

├─ write-file-atomic@5.0.1
└─ ylru@1.4.0
Done in 85.33s.
```
#### 查看项目package.json文件
```
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.2.4",
    "vue-router": "^4.6.4"
  }
```
#### 创建router文件夹
##### index.js
```
import { createRouter, createWebHashHistory } from "vue-router";
const routers = [{
    path: '/',//路径
    name: 'Home',//名字
    component: () => import('')
}
]
const router = createRouter({
    history: createWebHashHistory(),
    routers: routers//可以简写为routers
})
export default router
```
#### 一些常用配置
```
vite.config.js
```
```
  resolve: {
    alias: {
      '@': path.resolve(_dirname, 'src'),
    }
  },
```
#### main.js
```
import { createApp } from 'vue'
import './style.css'
import App from './App.vue'
import router from './router/index.js'// ./代表同级 ../代表上一级 @/根目录src需要手动配置
createApp(App).useAttrs(router).mount('#app')
```